# Global Instructions

## Git / Jujutsu push policy

Never run `git push`, `jj git push`, or any equivalent remote-publishing command unless the user has explicitly said to push in that same turn. Completing a commit or finishing a task is not implicit permission to push.
